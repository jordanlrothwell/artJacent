<section class="container container-flex m-3">
    <div class="row row-cols-6 row-cols-lg-3">
        <div class="col p-3">
            <div class="p-3 card">
                {{#each art as |artwork|}}
                <h2>{{name}}</h2>
                <img src="{{image}}" alt="{{name}}">
                <a class="btn btn-primary" href="#" role="button">Like</a>
                {{/each}}
            </div>
        </div>
    </div>
</section>