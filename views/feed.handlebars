<div class="container">
  <div class="">
      <div class="card-group mt-4 row">
          {{#each art as |artwork|}}
            <div class="mb-5 card-container col-sm-12 col-md-6 col-xl-3 mh-200">
              <img class="artwork-image card mh-100 w-100" src="{{image}}" alt="{{image}}">
              <div class="card-body"> 
              </div>
              <button type="button" id="delete-artwork" class="btn btn-secondary">Delete</button>
            </div>
          {{/each}}
      </div>
  </div>
</div>
